name: circle_flags
version: 5.0.1
description: Stub — no SVG assets, replaces circle_flags to remove 984K from bundle.
environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.0.0"
dependencies:
  flutter:
    sdk: flutter
flutter:
  # No assets declared — this intentionally omits all 246 SVG flags.
