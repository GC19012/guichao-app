import 'package:flutter/material.dart';

/// Proxy nodes list — VPN proxy module removed, this is a stub placeholder.
class ProxyNodesList extends StatelessWidget {
  const ProxyNodesList({super.key});

  @override
  Widget build(BuildContext context) {
    return const SliverFillRemaining(
      child: SizedBox.shrink(),
    );
  }
}
